apiVersion: v1
kind: Service
metadata:
  name: hospitality-service
spec:
  type: NodePort
  selector:
    app: hospitality
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081   # External port will be 30081
